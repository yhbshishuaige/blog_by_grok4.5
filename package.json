{
  "name": "weather-blog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A silky-smooth blog with weather-aware animations and 24h sky cycle",
  "scripts": {
    "build": "node scripts/build-posts.mjs",
    "watch": "node scripts/watch-posts.mjs",
    "predev": "node scripts/build-posts.mjs",
    "dev": "node scripts/start-server.mjs",
    "prestart": "node scripts/build-posts.mjs",
    "start": "node scripts/start-server.mjs",
    "new": "node scripts/new-post.mjs",
    "hooks": "git config core.hooksPath .githooks && chmod +x .githooks/pre-commit && echo 'git hooks → .githooks (提交 posts 时自动 build)'"
  },
  "devDependencies": {
    "highlight.js": "11.11.1",
    "marked": "15.0.12"
  }
}
